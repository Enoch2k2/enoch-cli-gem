require 'spec_helper'

describe Enoch::Cli::Gem do
  it 'has a version number' do
    expect(Enoch::Cli::Gem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
